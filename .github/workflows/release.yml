name: Release

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  shared:
    permissions:
      contents: write
      id-token: write
      pull-requests: write
    uses: tyler-mitchell/product-playbook/.github/workflows/pnpm-bumpy-release.yml@1cd007ad309a8f5aef35e620eab8b8132575f4f7
    with:
      packages: '[{"path":"libs/package-management","probe":"scripts/verify-published.mjs"}]'
